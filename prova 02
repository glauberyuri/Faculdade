#include<stdio.h>
#include<stdlib.h>
#include<string>
#include<iostream>
#include<locale.h>

using namespace std;
	
		typedef struct{
		float preco;
		char nome[30];
		int id;
		string desc;
		string qualidade;
		int quantidade;
		int idGrupo;
	}cad; 
class Funcionario  
{
	
	private:
		int idFun;
		string senha;
		
	public:
		string nome;
		int pItem;
		float pPreco;
	public :
	
	void alterarEstoque(int pItem, int pQuantidade) {
  
    item[pItem].quantidade = pQuantidade;
	
	}
	void modificarPreco(int pItem, float pPreco) {
    
	item[pItem].preco = pPreco;
	
	}
};
class Item : Funcionario{
	public:
		
	cad item;
	
	public:

	void listaProduto(int pQtProduto) {
		
    int i, qtProd;
    
    qtProd = pQtProduto;
	    	
		for (i = 0; i < qtProduto; i++) {
	  	 	cout <<  "NOME : \t "<< item[cont].nome <<"\n" << "Preço : /t "<<item[cont].preco << "\n id : \t" << item[cont].id << "\n Descricao :  \t"<< item[cont].desc << "\n Qualidade : \t"<<item[cont].qualidade <<"\n Grupo : \t" << item[cont].idGrupo;  
		 }
	    
	    fflush(stdout);
		}
		
	
	cad CriarProduto(){
	
		 	char opSup;
		 	int cont =0, qtProdCad =0;
		 	
		 	do{
			item[cont].id = cont;
			fflush(stdin);
			cout << "------------------------------- Informe os dados ---------------------------- \n";
			cout <<" NOME PRODUTO : ";
			getline(cin, item[cont].nome);
			cout << "\n\n Preco :";
			cin >> item[cont].preco;
			cout << "\n identificaçao produto :";
			cin >> item[cont].id;
			cout << " \n descriçao :   ";
			getline(cin, item[cont].desc);
			cout << " \n Qualidade Produto : ";
			cin >> item[cont].qualidade;
			cout << " \n Grupo : ";
			cin >> item[cont].idGrupo;
		  	
			  caixaAtual = caixaAtual - item[cont].preco;
      		
			  qtProdutoCad = qtProdCad + cont;
        	
			cont++;
        
		cout << "Deseja cadastrar um novo produto sim(s) ou não(n)?";
        fflush(stdout);
        getline(cin, opSup);
        fflush(stdin);
        
  		  } while (opSub == 's' || opSub == 'S');
  		  
    	return qtProdCad;
		
	}
	}; 
	
	};
	
	int main()
	{
		Item it;
		cad itV[i];	 
		it.item;
	 	int i;
		float preco; 
		string desc, qualidade;
		int op, id, idGrupo;
		
		
		cout << "Deseja Criar um Produto : 1 - SIM \t || \t 2 - NÃO || 3 Listar Produtos \n";
		cin >> op;
		do{
		
		switch(op)
		{
		case 1:
			
			break;
		}
		
			
		case 2:
		
			op=0;
			break;
			
		case 3:
			
			it.ImprimirProduto(it.item);
				
		break;
		
		default:
			break;
		
		}
		
	 	}while(op=!0);	
	
		}
	
