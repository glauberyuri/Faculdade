//Prova 01
#include<iostream>
#include<stdlib.h>
#include<string>
#include<locale.h>
#include<time.h>

using namespace std;

class Conta{
	private:
		int pop, cot, popCot, id;
		
	public:
		string  numConta, agencia, lim,nomeGer, cliente; 
		float  n, j,m, emp, empT, saldo, limiteT,cc,cp, max;
		int  idGer;
	public:
		
	float defLim(float){
	
	if ((saldo >= 1.500)&&( saldo <1.750 ))
	{
    limiteT = saldo*0.2+saldo; //VALOR DO limite
    cout << "\nValor do Aumento em 20% para limite : \n%f",limiteT;
	}
	else if ((saldo>=1.750)&&(saldo<2.000))
	{
    limiteT= saldo*0.4+saldo; //VALOR DO lIMITE
    cout <<"\nValor do Aumento em 40% para limite: \n%f",limiteT;
	}
	else if (saldo>=2.000)
	{
    limiteT= saldo*0.5 + saldo; //VALOR DO Limite
    cout << "\nValor do Aumento em 50 % para limite : \n%f", limiteT;
	}
    return (limiteT);
	}
	
	int defId()
	
		{
 		srand(time(NULL));

 		id = rand();
 		
		 return(id);
		}
		
		int defIdGerente(){
		

 		srand(time(NULL));

 		idGer = rand();
		 
		 cout << "Numero Identificacao Gerente :  %d\n", idGer;
		 
		 return(idGer);
		
		}
		string defGerente(){
			cout << "\n Informe nome Gerente autorizado sua conta : \n ";
			fflush(stdin);
			getline(cin, nomeGer);
			return(nomeGer);
		}
		double defEmp(float){
			
			if(emp <= max)
			{
			if(emp <= 1000)
			{
				j= emp*0.23*m;
				empT = emp+j; 
			}
			else if (emp <= 2000)
			 {			
			 j= emp*0.40*m;
			 empT = emp+j; 
			 }
			 else if(emp > 8000)
			 {
			 j= emp*0.60*m;
			 empT = emp+j; 
			 }
			 
			}
			else{
				printf("Emprestimo Recusado || Procure Gerente para verificar limite total");
			}
			return(empT);

			
		}
		int verificarTipo(){
				
				int op;
				cout << "Informe tipo de conta : | 1 | Corrente \t | 2 | Poupanca";
				cin >> op;
				switch(op)
				{
					case 1:
						cout << "|| Conta Corrente || ";
						
						break;
					case 2:
						cout << "|| Conta Poupança ||";
						
						break;
						default:
							cout << " INCORRETO ";
				}
				return (cot);
		}
		};
		
		main()
		{
			Conta c1;
			
			cout << "Valor requisitado Emprestimo  : ";
			cin >> max;
			float max=c1.defEmp(float);
			cout << "Emprestimo total : %f ", max;
			
			/*
			cout << "Valor total limite : %f", f;
			cout << "\n Informe saldo :";
			cin >> saldo;
			float f=c1.defLim(saldo);
			fflush(stdin);
			printf("saldo Total : \n %d",saldo);

			
			
			printf("limite da Conta : \n %d");
			cout << " \n Informe Valor total emprestimo :";
			cin >> emp;
			cout << " \n Informe quantidade de parcelas :";
			cin >> m;
			cout << "Numero Identificacao Conta :  %d\n", id;
			*/
		}
